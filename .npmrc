# NPM Configuration for GesNeu Project

# Use legacy peer deps to avoid warnings with React 19 + older packages
# This is safe because React 19 is backward compatible with React 18 APIs
legacy-peer-deps=true

